<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
</head>
<body>
 <!-- <form action="/post" method="post" name="join">
    이름 <input type="text" name="name">
    <fieldset>
        <legend>성별</legend>
     <input type="radio" name="gedner" value="남자">남자
     <input type="radio" name="gedner" value="여자">여자
    </fieldset>

    <fieldset>
        <legend>생년월일</legend>
        <select id="year" name="year"> 

        </select> 년
        <select id="month" name="month"> 

        </select>월
        <select id="day" name="day" > 

        </select>일

    </fieldset>

    <fieldset>
        <legend>관심사</legend>
        <input type="checkbox" name="interest" value="travel">여행
        <input type="checkbox" name="interest" value="fassion">패션
        <input type="checkbox" name="interest" value="food">음식
    </fieldset>
    <button type="submit">회원가입</button>

    <fieldset>
        <legend>선택</legend>
        좋아하는 색 <input type="color" name="color" value="#0000ff">
        좋아하는 숫자<input type="number" name="number">
        
    </fieldset>

    
 </form>
 -->



 <form action="/post/2" method="post" >
    <h2>validation</h2>
    <fieldset>
    아이디 <input type="text" name="id" pattern="^([a-z0-9]){5,}$" required> <br>

    비밀번호 <input type="password" name="pw" required pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[$@$!%?&])[A-Za-z\d$@$!%?&]{8,}" title = "소문자,대문자,특수문자,숫자를 모두 한번에 사용하여 8글자이상으로 만드세요"><br>
    전화번호<input type="text" name="phone" title="010-0000-0000 형식" pattern="^\d{3}-\d{3,4}-\d{4}$/" required/><br>
    이름<input type="text" name="name" required><br>
    성별 <input type="radio" name="gedner" value="남자" required>남자
    <input type="radio" name="gedner" value="여자" required>여자
    <button type="submit">회원가입</button>
</fieldset>
 </form>

 <script>
    const now = new Date();
    const year = now.getFullYear();
    const month = now.getMonth();
    const dat = now.getDate();

    for( let i = 1900; i<= year; i++){ 
        $('#year').append(
            `<option value="${i}">${i}년</option>`
        )
     } 
    for( let p = 1; p<= 12; p++){
        $('#month').append(
            `<option value="${p}">${p}월</option>`
        )
    }
    for( let d = 1; d<= 31; d++){
        $('#day').append(
            `<option value="${d}">${d}일</option>`
        )
    }


 </script>
</body>
</html>